info:
  name: Get Lab by Slug
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/labs/{{labSlug}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
