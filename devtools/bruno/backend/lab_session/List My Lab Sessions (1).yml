info:
  name: List My Lab Sessions (1)
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/lab-sessions"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
