info:
  name: Stop Lab Session
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{baseUrl}}/lab-sessions/{{labSessionId}}"
  auth:
    type: bearer
    token: "{{accessToken}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
