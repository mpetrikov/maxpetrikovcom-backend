info:
  name: Get Lab Session
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/lab-sessions/{{labSessionId}}"
  auth:
    type: bearer
    token: "{{accessToken}}"

runtime:
  variables:
    - name: labSessionId
      value: f7e4c850-2df7-4d85-8bac-4a850eae2047

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
